import 'package:flutter/material.dart';

class TabJan extends StatelessWidget {
  const TabJan({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(

    );
  }
}
